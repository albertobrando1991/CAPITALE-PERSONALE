import { ProgressBar } from "../ProgressBar";

export default function ProgressBarExample() {
  return (
    <div className="w-64">
      <ProgressBar value={65} label="Progresso Studio" />
    </div>
  );
}
